apiVersion: apps/v1
kind: Deployment
metadata:
  name: anrs
  labels:
    app: anrs
spec:
  replicas: 15
  selector:
    matchLabels:
      app: anrs
  template:
    metadata:
      labels:
        app: anrs
    spec:
      containers:
      - name: anrs
        image: anrs125/webhook:v1.25
        ports:
        - containerPort: 80
